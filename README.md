# Assignment-FTP-Downloader
In Java: Download a certain ZIP-file, containing a CSV, to a user-specified folder, unzip it, process the CVS-file (such that all entries with id>6500000 are deleted) and saved as a new file.
